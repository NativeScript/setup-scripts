branches:
  only:
    - master
matrix:
  include:
    - stage: "Build and Test"
      os: windows
      script:
        - echo "Test"
    - os: osx
      env:
        - BuildiOS="12.0"
        - Xcode="10.0"
      osx_image: xcode10.0
      language: node_js
      node_js: "8"
      script:
        - echo "Test"
